{
  "name": "<%= _.slugify(slug) %>",
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.6.0",
    "gulp-shell": "^0.2.4",

  },
  "engines": {
    "node": ">=0.10.0"
  }
}